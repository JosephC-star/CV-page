const friend = "BRUTUS";
const shiftValue = 3;

const alphabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";

// let encryptedName ="";

// for (let i=0; i < friend.length; i++){
//     const currentLetter = friend[i];
//     const currentIndex = alphabet.indexOf(currentLetter.toLowerCase());
//     const newIndex = (currentIndex + shiftValue) % alphabet.length;
//     encryptedName += alphabet[newIndex]. toUpperCase();
// }

// Loops make it so programmers dont have to write redundant code. it automates the process without the extra code.

// the % in the code ensures when we get to z it will wrap around to start at a again.
